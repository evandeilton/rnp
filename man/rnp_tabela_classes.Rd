% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribuicoes.R
\name{rnp_tabela_classes}
\alias{rnp_tabela_classes}
\title{Tabela de classes (frequencias agrupadas)}
\usage{
rnp_tabela_classes(
  x,
  regra = c("sturges", "scott", "fd", "rice", "yule", "sqrt", "fixa"),
  k = NULL,
  amplitudes = NULL,
  right = TRUE,
  digits = 4L
)
}
\arguments{
\item{x}{Vetor numerico.}

\item{regra}{String: \code{"sturges"}, \code{"scott"}, \code{"fd"},
\code{"rice"}, \code{"yule"}, \code{"sqrt"}, \code{"fixa"}.}

\item{k}{Inteiro. Numero de classes (quando \code{regra = "fixa"}).}

\item{amplitudes}{Vetor opcional de quebras customizadas. Sobrescreve regra.}

\item{right}{Logico. Intervalo fechado a direita (ver \code{\link[=cut]{cut()}}).}

\item{digits}{Inteiro. Casas decimais.}
}
\value{
tibble com colunas: \code{classe}, \code{lim_inf}, \code{lim_sup},
\code{ponto_medio}, \code{fa}, \code{fr}, \code{fa_acumulada},
\code{fr_acumulada}.
}
\description{
Constroi tabela de frequencias para variaveis quantitativas continuas,
com regra de amplitude configuravel: Sturges, Scott, Freedman-Diaconis,
Rice, Yule, sqrt(n) ou numero fixo de classes.
}
\examples{
rnp_tabela_classes(rnorm(500))
rnp_tabela_classes(faithful$eruptions, regra = "scott")
rnp_tabela_classes(1:100, regra = "fixa", k = 5)
}
